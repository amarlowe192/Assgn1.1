import type { NumberArray } from 'cheminfo-types';
interface XMedianOptions {
    /**
     * If true, the function will return the average of the two middle values.
     * If false, the function will return the lower index of the two middle values.
     * @default false
     */
    exact?: boolean;
    /**
     * First index (inclusive) of the range to consider.
     * @default 0
     */
    fromIndex?: number;
    /**
     * Last index (exclusive) of the range to consider.
     * @default input.length
     */
    toIndex?: number;
}
/**
 * Calculates the median of an array.
 * @param input - array containing values.
 * @param options - options.
 * @returns median.
 */
export declare function xMedian(input: NumberArray, options?: XMedianOptions): number;
export {};
//# sourceMappingURL=xMedian.d.ts.map